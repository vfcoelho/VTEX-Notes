query productReleaseDate($slug: String){
    product(slug: $slug) {#calls product resolver from vtex.search-graphql
        releaseDate
    }
}
query productViewCount($slug: String){
    productInfo(slug: $slug) {#calls product resolver from vtex.search-graphql
        releaseDate
    }
}